# Getting Started

<Request default-tab="0" show-lines="true" tabs={["request"]}>
  ```javascript request
  // add request here
  ```
</Request>

Welcome to the **MDX editor demo**.\
This page shows how Markdown and custom components can work together.

<Callout kind="note" collapsed="false">
  This is a custom callout component rendered inside MDX.
  You can pass props and render rich content here.
</Callout>

## Installation

Run the following command:

```bash
npm install my-library
```

## Usage

<Tabs>
  <Tab title="JavaScript">
    ```js
    import { createClient } from "my-library"

    const client = createClient({
      apiKey: "your-key"
    })

    client.start()
    ```
  </Tab>

  <Tab title="Python">
    ```python
    from my_library import Client

    client = Client(api_key="your-key")
    client.start()
    ```
  </Tab>
</Tabs>

## Notes

- MDX supports **Markdown**

- It also supports **React components**

- You can mix them freely

<Callout kind="note" collapsed="false">
  Avoid putting large business logic inside MDX files.
</Callout>

